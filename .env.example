# ============================================================
# APEX AI ENGINE — Environment Configuration
# Garcar Enterprise | Copy to .env and fill in values
# Placeholders only — NEVER commit real secrets
# ============================================================

# --- App ---
APP_ENV=production
APP_SECRET_KEY=change-me-to-64-char-random-string
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=false

# --- Local Compose Postgres (db service: postgres:16-alpine) ---
POSTGRES_USER=apex
POSTGRES_PASSWORD=apex
POSTGRES_DB=apex
# Use host "db" inside compose; localhost when running API on host
DATABASE_URL=postgresql://apex:apex@db:5432/apex

# --- OpenAI ---
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
OPENAI_FALLBACK_MODEL=gpt-3.5-turbo

# --- Supabase (optional cloud DB; overrides local DATABASE_URL when set in deploy) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# --- Redis / Celery (redis service) ---
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# --- Stripe ---
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID_STARTER=price_...
STRIPE_PRICE_ID_PRO=price_...
STRIPE_PRICE_ID_ENTERPRISE=price_...

# --- MARS API (Real Estate) ---
MARS_API_KEY=your-mars-api-key
MARS_API_URL=https://api.marsrealty.io/v1

# --- Webhooks ---
WEBHOOK_SECRET=your-webhook-signing-secret
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret

# --- Notion ---
NOTION_TOKEN=secret_...
NOTION_DATABASE_ID=your-notion-db-id

# --- Agent / Worker Config ---
SCOUT_AGENT_CONCURRENCY=5
ANALYST_AGENT_CONCURRENCY=3
EXECUTOR_AGENT_CONCURRENCY=2
MONETIZER_AGENT_CONCURRENCY=2
WORKER_CONCURRENCY=
AGENT_MAX_RETRIES=3
AGENT_RETRY_BACKOFF=2

# --- Security ---
JWT_SECRET=change-me-jwt-secret-64-chars
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
CORS_ORIGINS=["http://localhost:3000","https://your-domain.com"]

# --- Sentry (optional) ---
SENTRY_DSN=
